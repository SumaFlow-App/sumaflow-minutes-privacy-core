# test/fixtures/pubspec.yaml
# A COMPLIANT fixture pubspec — contains no known-telemetry packages.
# Used to prove privacyManifestTests() passes a clean dependency list.
name: sumaflow_minutes_fixture
description: "Compliant fixture pubspec for the privacy-manifest regression test."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  record: ^5.2.0
  sqflite_sqlcipher: ^3.1.0
  flutter_secure_storage: ^9.2.2
  cryptography: ^2.7.0
  pdf: ^3.11.0
  printing: ^5.13.0
  permission_handler: ^11.3.1
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
