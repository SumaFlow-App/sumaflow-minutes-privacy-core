<!-- test/fixtures/AndroidManifest.xml
     A COMPLIANT fixture manifest, mirroring the shape of the main app's
     android/app/src/main/AndroidManifest.xml. Used to prove that
     privacyManifestTests() passes a clean input. Keep this in sync with
     the main app's permission set when it legitimately changes. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required for the core flow per PRD §7.1 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- INTERNET + ACCESS_NETWORK_STATE are gated to the opt-in,
         Wi-Fi-only model downloads, both from huggingface.co:
           - the Gemma 4 E2B model download (TechSpec §5.3, §10), and
           - the optional Whisper "small.en" model download (TechSpec §4.4).
         Each download is SHA256-verified against a pinned HuggingFace commit.
         Inference itself runs entirely on-device with no outbound calls.
         See PRD §5. This mirrors the main app's manifest; the rationale
         comment is what privacyManifestTests() requires alongside INTERNET. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:label="Minutes"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop" />
        <service
            android:name=".recording.RecordingForegroundService"
            android:foregroundServiceType="microphone"
            android:exported="false" />
    </application>
</manifest>
